// game-card.spec.ts — one game card on the board (GameCard).
//
// Step 4 skeleton (test.fixme = planned). Filled in at Step 9 (needs a seeded
// game in the backlog).

import { test } from "@playwright/test";

test.describe("Game card", () => {
  test.fixme("clicking the title navigates to the game detail page", () => {});
  test.fixme("changing the status select moves the card to another column", () => {});
  test.fixme("the delete button opens a confirmation modal", () => {});
  test.fixme("cancelling the confirmation keeps the game on the board", () => {});
  test.fixme("confirming deletion removes the game from the board", () => {});
});
