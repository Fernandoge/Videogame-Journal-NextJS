// profile.spec.ts — the profile dashboard with stats (/profile).
//
// Step 4 skeleton (test.fixme = planned). Filled in at Step 9 (all stats are
// derived from seeded data).

import { test } from "@playwright/test";

test.describe("Profile page", () => {
  test.fixme("shows the user's name and email", () => {});
  test.fixme("shows the four overview stat cards", () => {});
  test.fixme("shows the status breakdown counts", () => {});
  test.fixme("lists recent play sessions", () => {});
  test.fixme("lists recently completed games", () => {});
  test.fixme("lists written reviews", () => {});
  test.fixme("shows empty states when there is no data", () => {});
});
